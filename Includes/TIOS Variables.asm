.pvar ufp6.2, contrast		; Contrast level.

.pvar ubyte, penCol, penRow	; Pen position.
.pvar ubyte, curCol, curRow	; Cursor position.
.pvar asc, curUnder

.pvar tifloat, OP1, OP2, OP3	; Floating-point variables.
.pvar tifloat, OP4, OP5, OP6	; 

.pvar ubyte, fmtDigits		; Digit format (number of points).

.pvar ubyte, curGX, curGY	; Current graph cursor location.
.pvar ubyte, curGStyle		; Current graph cursor style.